const deskNav = document.querySelectorAll('.navbar--desktop__menu li a');
const mobNav = document.querySelectorAll('.navbar--mobile li a');

const navbarIcon = document.querySelector('.navbar--mobile svg');
const navbarMenu = document.querySelector('.navbar--mobile__menu');
const navButtons = document.querySelectorAll('.navbar--mobile li a');

const languageSelector = document.querySelector('.language--selector--selected p');
const languageOption = document.querySelector('.language--selector--option');

const headerParagraph = document.querySelector('header p');

const techTitle = document.querySelector('.main-technologies-container h2');

const aboutTitle = document.querySelector('.main-about-container h2');
const aboutPublished = document.querySelectorAll('.about-container__published');
const aboutDesc = document.querySelectorAll('.about-container__text');

const portfolioTitle = document.querySelector('.portfolio-container--title');
const projectTech = document.querySelector('.card--content--technologies');
const projectTitle = document.querySelector('.portfolio--card--content h3');
const projectDesc = document.querySelector('.card--content--description');

const contactTitle = document.querySelector('footer h2');